services:
  - type: web
    name: qalam-telegram-bot
    env: python
    buildCommand: |
      apt-get update && apt-get install -y openjdk-11-jdk
      pip install -r requirements.txt
    startCommand: python QalamBot.py



 