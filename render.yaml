services:
  - type: web
    name: qalam-telegram-bot
    env: docker
    buildCommand: # This is not needed when using Docker, Render uses the Dockerfile
    startCommand: python QalamBot.py
    
    